# ── Raw NeBULA dataset (never commit — 21GB) ─────────────────────────────
data/
*.eeg
*.vhdr
*.vmrk

# ── Preprocessed arrays (large, regeneratable from epoch.py) ─────────────
preprocessed/
subject_dependant/preprocessed_sub_dep/
*.npy
*.npz
*.pkl
*.h5

# ── Model weights (large, regeneratable from training scripts) ────────────
*.pt
*.pth

# ── Keep results JSON and plots — useful for reproducibility ─────────────
# models_sub_independant/results/ → committed
# subject_dependant/results_dep/ → committed
# eda/diagnostics_eeg/ → committed
# eda/diagnostics_emg/ → committed
# eda/feasibility_plots/ → committed
# temporal_analysis/timing_results/ → committed

# ── Virtual environments ──────────────────────────────────────────────────
venv/
.venv/
env/
venv313/

# ── Python cache ──────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.py[cod]

# ── Jupyter ───────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── macOS ─────────────────────────────────────────────────────────────────
.DS_Store

# ── IDE ───────────────────────────────────────────────────────────────────
.vscode/
.idea/

# ── Misc ──────────────────────────────────────────────────────────────────
tmp/
old/